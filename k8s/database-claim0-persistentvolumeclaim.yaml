apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: dso-namespace
  name: database-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
status: {}
